{
  "name": "@lobechat/file-loaders",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/file-loaders/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec tsc --project packages/file-loaders/tsconfig.json"
      },
      "outputs": [
        "{workspaceRoot}/packages/file-loaders/dist"
      ],
      "cache": true
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest run packages/file-loaders"
      },
      "cache": true,
      "dependsOn": [
        "build"
      ]
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf packages/file-loaders/dist"
      }
    }
  },
  "tags": [
    "scope:@lobechat/file-loaders"
  ]
}
