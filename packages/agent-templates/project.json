{
  "name": "@lobechat/agent-templates",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/agent-templates/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec tsc --project packages/agent-templates/tsconfig.json"
      },
      "outputs": [
        "{workspaceRoot}/packages/agent-templates/dist"
      ],
      "cache": true
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest run packages/agent-templates"
      },
      "cache": true,
      "dependsOn": [
        "build"
      ]
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf packages/agent-templates/dist"
      }
    }
  },
  "tags": [
    "scope:@lobechat/agent-templates"
  ]
}
