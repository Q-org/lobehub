{
  "name": "@lobechat/builtin-tool-memory",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/builtin-tool-memory/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec tsc --project packages/builtin-tool-memory/tsconfig.json"
      },
      "outputs": [
        "{workspaceRoot}/packages/builtin-tool-memory/dist"
      ],
      "cache": true,
      "dependsOn": [
        "@lobechat/memory-user-memory:build",
        "@lobechat/prompts:build"
      ]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest run packages/builtin-tool-memory"
      },
      "cache": true,
      "dependsOn": [
        "build"
      ]
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf packages/builtin-tool-memory/dist"
      }
    }
  },
  "tags": [
    "scope:@lobechat/builtin-tool-memory"
  ]
}
